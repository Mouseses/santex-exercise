server:
  port: 8080

spring:
  profiles:
    active: dev
  application:
    name: demo
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
